class Rect {
  top;
  left;
  width;
  height;

  constructor({ top, left, width, height }) {
    this.top = top;
    this.left = left;
    this.width = width;
    this.height = height;
  }
}

module.exports = Rect;
